/**
 * Placeholder for the Service Worker registration helper.
 * Real implementation lands in POC-6 (RESEARCH.md §16).
 */

export function registerServiceWorker(): void {
  // No-op during scaffolding — vite-plugin-pwa will inject its own register
  // snippet in production. See `vite.config.ts` (`injectRegister: false`).
}
